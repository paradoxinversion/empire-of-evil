// Config is validated once at engine initialization via Zod schemas.
// After validation the typed config is available as module-level constants.

export const loadConfig = (): void => {
  // TODO: load and validate all JSON config files via Zod schemas
};
